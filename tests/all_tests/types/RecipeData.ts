export type RecipeData = {
  name: string;
  ingredients: string;
  instructions: string;
  imagePath?: string;
  time: number;
  category: string;
  cuisine: string;
  difficulty: string;
  seasonality: string;
  specialDiet: string;
};
